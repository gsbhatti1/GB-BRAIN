``` pinescript
/*backtest
start: 2022-12-05 00:00:00
end: 2023-12-11 00:00:00
period: 1d
basePeriod: 1h
exchanges: [{"eid":"Futures_Binance","currency":"BTC_USDT"}]
*/

// Author Obarut
//@version=5
strategy("Ichimoku Strategy With Money Management", overlay=true)

// Inputs
ts_period = input.int(9, minval=1, title="Tenkan-Sen Period")
ks_period = input.int(26, minval=1, title="Kijun-Sen Period")
ssb_period = input.int(52, minval=1, title="Senkou-Span B Period")
cs_offset = input.int(26, minval=1, title="Chikou-Span Offset")
ss_offset = input.int(26, minval=1, title="Senkou-Span Offset")

// Back Testing Period
from_day = input.int(title="Start Date", minval=1, maxval=31)
from_month = input.int(title="Start Month", minval=1, maxval=12)
from_year = input.int(title="Start Year", minval=1800, maxval=2100)
to_day = input.int(title="En Date", minval=1, maxval=31)
to_month = input.int(title="End Month", minval=1, maxval=12)
to_year = input.int(title="End Year", minval=1800, maxval=2100)

start_time = timestamp(from_year, from_month, from_day, 00, 00)
end_time = timestamp(to_year, to_month, to_day, 00, 00)
timewindow = time >= start_time and time <= end_time

middle(len) => math.avg(ta.lowest(len), ta.highest(len))

// Ichimoku Components
tenkan = middle(ts_period)
kijun = middle(ks_period)
senkouA = math.avg(tenkan, kijun)
senkouB = middle(ssb_period)

atr = ta.atr(14)
ss_above = math.max(senkouA[ss_offset - 1], senkouB[ss_offset - 1])
ss_below = math.min(senkouA[ss_offset - 1], senkouB[ss_offset - 1])

// Price Distance From Tenkan
distance = close - tenkan

// Price Distance from Kijun
distancek = close - kijun

// Entry/Exit Signals
tk_cross_kijun_bull = ta.crossover(tenkan, kijun)
tk_cross_kijun_bear = ta.crossunder(tenkan, kijun)
cs_cross_bull = ta.mom(close, cs_offset - 1) > 0
cs_cross_bear = ta.mom(close, cs_offset - 1) < 0
price_above_kumo = close > ss_above
pbsenkA = close < ss_above
pasenkB = close > ss_below
price_below_kumo = close < ss_below
future_kumo_bull = senkouA > senkouB
future_kumo_bear = senkouA < senkouB

// Price Distance From Tenkan
disbull = distance < 2 * atr

// Price Distance From Kijun
disbullk = distancek < 3 * atr

// Entry Conditions
long_entry = tk_cross_kijun_bull and price_above_kumo and future_kumo_bull and disbull and disbullk

// Exit Conditions
long_exit = tk_cross_kijun_bear or (close > ss_above * 1.05) or (profit_percentage(close, open) > 30) or (loss_percentage(close, open) < -3)

plotshape(series=long_entry, title="Long Entry", location=location.belowbar, color=color.green, style=shape.labelup, text="Buy")

// Plot Exit Signals
plotshape(series=long_exit, title="Long Exit", location=location.abovebar, color=color.red, style=shape.labeldown, text="Sell")
```

This code snippet is the complete Pine Script for implementing the Ichimoku Kinko Hyo strategy with money management. It includes input parameters for various periods and offsets, as well as conditions for entry and exit based on the signals generated by the Ichimoku indicators.