> Name

Moving Average-Trend_cryptocurrency_strategyV02

> Author

Taiji

> Strategy Description

@Taiji   QQ7650371
#Moving Average/Trend Strategy
#Buy after the death cross is at a low and rebounds by a certain amount
#Sell after the golden cross reaches a high and falls by a certain amount

> Strategy Arguments


|Argument|Default|Description|
|----|----|----|
|FastPeriod|2|Opening Fast Line Period|
|SlowPeriod|4|Opening Slow Line Period|
|EnterPeriod|true|Opening Observation Period|
|x|------------------------------------------------------------------------------|Separator|
|ExitFastPeriod|2|Closing Fast Line Period|
|ExitSlowPeriod|4|Closing Slow Line Period|
|ExitPeriod|2|Closing Observation Period|
|xx|------------------------------------------------------------------------------|Separator|
|PositionRatio|0.5|Position Ratio|
|Interval|10|Polling Cycle (Seconds)|
|xxx|------------------------------------------------------------------------------|Separator|
|MAType|0|Moving Average Type: TA.EMA|TA.MA|


> Source (python)

```python
#!/usr/local/bin/python
#-*- coding: UTF-8 -*-
#Moving Average/Trend Strategy
#Buy after the death cross is at a low and rebounds by a certain amount
#Sell after the golden cross reaches a high and falls by a certain amount

# FastPeriod=3 #Opening Fast Line Period
# SlowPeriod=7 #Opening Slow Line Period
# EnterPeriod=1       #Opening Observation Period
# ExitFastPeriod=3 #Closing Fast Line Period
# ExitSlowPeriod=7 #Closing Slow Line Period
# ExitPeriod=2        #Closing Observation Period
# PositionRatio=0.5 #Position Ratio
# Interval=10 #Polling Cycle
# MAType=0 #Moving Average Type TA.EMA|TA.MA


import types
array = [TA.EMA,TA.MA]
_MACalcMethod = array[MAType]
def Cross(a,b):   #Calculate Moving Average Method
    crossNum = 0
    arr1 = []
    arr2 = []
    if(type(a) == types.ListType and type(b) == types.ListType):
        arr1 = a
        arr2 = b
    else:
        records = None
        while True:
            records = exchange.GetRecords()
            if(records and len(records) > a and len(records) > b):
                break
            Sleep(Interval)
        arr1 = _MACalcMethod(records,a)
        arr2 = _MACalcMethod(records,b)
    if(len(arr1) != len(arr2)):
        raise Exception("array length not equal")
    for i in range(len(arr1) - 1,-1,-1):
        if((type(arr1[i]) != types.IntType and type(arr1[i]) != types.FloatType) or (type(arr2[i]) != types.IntType and type(arr2[i]) != types.FloatType) ):
            break
        if(arr1[i] < arr2[i]):
            if(crossNum > 0):
                break
            crossNum -= 1
        elif(arr1[i] > arr2[i]):
            if(crossNum < 0):
                break
            crossNum += 1
        else:
            break
    return crossNum

import datetime
def Caltime(date1,date2):
    try:
        date1=time.strptime(date1,"%Y-%m-%d %H:%M:%S")
        date2=time.strptime(date2,"%Y-%m-%d %H:%M:%S")
        date1=datetime.datetime(date1[0],date1[1],date1[2],date1[3],date1[4],date1[5])
        date2=datetime.datetime(date2[0],date2[1],date2[2],date2[3],date2[4],date2[5])
        return date2-date1
    except Exception as ex:
        Log('except Exception Caltime:',ex)
        return "except Exception"

import time
start_time = time.localtime(time.time()) #time.clock()
start_time_str = time.strftime("%Y-%m-%d %H:%M:%S", start_time)
buy_price = 0 #Buy Price
buy_qty = 0  #Buy Quantity
gains = 0  #Profits

def my_buy(): #Opening Position
    try:
        global buy_price, buy_qty
        initAccount = ext.GetAccount()  # Trading template export function, obtain account status, save strategy running account initial state
        opAmount = 1
        # Before opening a position, check if there are any coins, buy if not
        if int(initAccount.Stocks) > 1:
            if buy_price < 1:
                buy_price = _C(exchange.GetTicker).Last
                buy_qty = initAccount.Stocks
            Log('Opening information: Stocks in the account:', initAccount.Stocks, 'Clearing stocks','--Opening details:', initAccount)
            return 1
        if int(initAccount.Stocks) < 1:
            if int(str(initAccount.Stocks).replace('0.', '')) >= 1:
                if buy_price < 1:
                    buy_price = _C(exchange.GetTicker).Last
                    buy_qty = initAccount.Stocks
                Log('Opening information: Stocks in the account:', initAccount.Stocks, 'Clearing stocks','--Opening details:', initAccount)
                return 1

        if int(str(initAccount.Stocks).replace('0.', '')) == 0:
            # opAmount = 1
            opAmount = _N(initAccount.Balance * PositionRatio, 3)  # Buying quantity
            Log("Opening without coins, opening purchase of %s yuan" % (str(opAmount)))  # Generate log
        #     else:
        #         opAmount = _N(initAccount.Stocks * PositionRatio, 3)  # Get transaction quantity
        # else:
        #     opAmount = _N(initAccount.Stocks * PositionRatio, 3)  # Get transaction quantity
        Dict = ext.Buy(opAmount)  # Buy ext.Buy
        if Dict:  # Confirm successful opening position
            buy_price = Dict['price']  # Buy price  #{'price': 4046.446, 'amount': 1.5}
            buy_qty = Dict['amount']  # Buy quantity
            print_log(1, initAccount, Dict)
            return 1
        return 0

    except Exception as ex:
        Log('except Exception my_buy:', ex)
        return 0

outAccount = ext.GetAccount()  # Initialize information
def print_log(k_p, Account, Dict):
    try:
        global outAccount
        name = ""
        if k_p:
            LogProfit(_N(gains, 4), 'Opening information: Money:', Account.Balance, '--Coins:', Account.Stocks, '--Opening details:', Dict)
            name = "Opening"
        else:
            LogProfit(_N(gains, 4), 'Closing information: Money:', Account.Balance, '--Coins:', Account.Stocks, '--Closing details:', Dict)
            name = "Closing"
        endAccount = ext.GetAccount()  # Initialize information
        date1 = time.strftime("%Y-%m-%d %H:%M:%S", time.localtime(time.time()))
        LogStatus("Initial investment on 2016/9/16, initial capital 2000 yuan\r\n",
                  "This initialization status:", outAccount,
                  "\r\nCurrent operational status:", endAccount,
                  "\r\nStart time of this operation:%s, running time:%s\r\n" % (start_time_str, Caltime(start_time_str, date1)),
                  "This profit:%s\r\n" % (str(gains)),
                  "Current status:%s--Money:%s--Coins:%s\r\n" % (str(name), str(Account.Balance), str(Account.Stocks)),
                  "Update time:%s" % (date1))  # Testing
    except Exception as ex:
        Log('except Exception print_log:', ex)


def my_sell(): #Closing Position
    try:
        global buy_price, buy_qty, gains, start_time
        nowAccount = ext.GetAccount()  # Trading template export function, get account information
        if _C(exchange.GetTicker).Last > buy_price + 4:  # The current price must be greater than the opening price
            Dict = ext.Sell(nowAccount.Stocks)
            if Dict:
                sell_gains = (Dict['price'] - buy_price) * Dict['amount']
                gains = gains + sell_gains
                buy_price = 0  # Buy price
                buy_qty = 0  # Buy quantity
                print_log(0, nowAccount, Dict)
    except Exception as ex:
        Log('except Exception my_sell:', ex)
```