Name

Chart Interface Information Display Example 1

Author

Tai Chi

Strategy Description

11111111



Source (javascript)

``` javascript
//==========================================
//API: Simple example of using Chart function (drawing function)
var chart = { // This chart is an object in JS language. Before using the Chart function, we need to declare an object variable chart that configures the chart.
__isStock: true, // Mark whether it is a general chart. If you are interested, you can change it to false and run it to see.
tooltip: {xDateFormat: '%Y-%m-%d %H:%M:%S, %A'}, // Zoom tool
title : { text : 'Spread Analysis Chart'}, // title
rangeSelector: { // Select range
buttons: [{type: 'hour',count: 1, text: '1h'}, {type: 'hour',count: 3, text: '3h'}, {type: 'hour', count: 8, text: '8h'}, {type: 'all',text: 'All'}],
selected: 0,
inputEnabled: false
},
xAxis: { type: 'datetime'}, // The horizontal axis of the coordinate axis is: x-axis, the currently set type is: time
yAxis: { // The vertical axis of the coordinate axis is the y-axis. The default value is adjusted with the data size.
title:{text: 'Price difference'}, // title
opposite:false, // Whether to enable the right vertical axis
},
series: [ // Data series, this attribute saves each data series (line, K-line chart, label, etc..)
{name:"Exchange0",id:"0,Buy",color:'#FF3030',data:[]}, //The index is 0, and the data array stores the data of the index series
{name:"Exchange 1",id:"1,Buy",dashStyle:'shortdash',data:[]}, //The index is 1, dashStyle is set: 'shortdash', that is, set a dash line.
{name:"Exchange 2",id:"2,Buy",color:'#912CEE',data:[]},
//RGB color comparison table http://www.114la.com/other/rgb.htm
]
};
//==========================================
//Get the current time
function getNowFormatDate() {
var date = new Date();
var seperator1 = "-";
var seperator2 = ":";
var month = date.getMonth() + 1;
var strDate = date.getDate();
if (month >= 1 && month <= 9) {
month = "0" + month;
}
if (strDate >= 0 && strDate <= 9) {
strDate = "0" + strDate;
}
var currentdate = date.getFullYear()+seperator1+month+seperator1+strDate+" "+date.getHours()+seperator2+date.getMinutes()+seperator2+date.getSeconds();
return currentdate;
}


function main() {
//==========================================
//Draw a picture https://www.botvs.com/bbs-topic/581
var ObjChart = Chart(chart); // Call the Chart function to initialize the chart.
ObjChart.reset(); // Clear
while(true){
var nowTime = new Date().getTime(); //Get the timestamp,
var Buy_0 = _C(exchanges[0].GetTicker).Buy; //Get the market data of platform 0 and buy one price
var Buy_1 = _C(exchanges[1].GetTicker).Buy; //Get the market data of platform 1 and buy one price
var Buy_2 = _C(exchanges[2].GetTicker).Buy; //Get the market data of platform 2 and buy one price
ObjChart.add([0, [nowTime,Buy_0]]); // Use the timestamp as the X value and the buy price as the Y value. Pass in the data sequence at index 0.
ObjChart.add([1, [nowTime,Buy_1]]); // Same as above.
ObjChart.add([2, [nowTime,Buy_2]]); // Same as above.
ObjChart.update(chart); // Update the chart to display.
Sleep(2000);
}
//==========================================

}

```

Detail

https://www.fmz.com/strategy/36026

Last Modified

2017-02-18 15:29:31